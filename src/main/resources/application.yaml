spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/baza?serverTimezone=Europe/Warsaw&createDatabaseIfNotExist=true
  jpa:
    hibernate:
      ddl-auto: update
